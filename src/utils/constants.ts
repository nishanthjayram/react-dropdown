export const ITEM_HEIGHT = 36; // Height of each option row (px)
export const CONTAINER_HEIGHT = 200; // Height of the scrollable container (px)
export const VIRTUALIZATION_THRESHOLD = 100; // Number of options to enable virtualization
